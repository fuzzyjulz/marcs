%nav#memberNavBar.navbar.navbar-inverse.center-block
 .container-fluid
  .navbar-header
  #marcs-navbar.collapse.navbar-collapse
  .navbar-center
   %ul.nav.navbar-nav
    %li{class: (controller.is_a? UsersController and action_name == "home") ? "active" : ""}
     %a{href: home_user_path} Profile
    -if can? :view_club_trainers, current_user
     %li{class: (controller.is_a? UsersController and action_name == "trainers") ? "active" : ""}
      %a.membersTrainers{href: trainers_user_path} Trainers
    -if can? :view_committee_members, current_user
     %li{class: (controller.is_a? UsersController and action_name == "committee") ? "active" : ""}
      %a{href: committee_user_path} Committee
    -if can? :view_minutes, current_user
     %li{class: (controller.is_a? MinutesController and action_name == "index") ? "active" : ""}
      %a{href: minutes_path} Minutes
    -if can? :renew_membership, current_user
     %li{class: (controller.is_a? MembershipYearsController and ["renew","fees","update","show"].include? action_name) ? "active" : ""}
      %a.renewMembership{href: renew_user_membership_years_path} Renew Membership
